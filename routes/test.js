/**
 * javascript array1.map(function(value,index,array1) 배열이나, json 등 object list
 * 데이터를 순회하면서 map(key,value) 형태로 돌려 준다.
 */

var data = {1, 2, 3, 4, 5, 6};
var oldTime = '';

/**
 * 
 */
var datas = data.map(function(d) {
	return d
})
