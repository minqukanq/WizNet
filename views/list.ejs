<!DOCTYPE html>
<html>
<% include ../public/include/mobileHeader.html %>
<script>
$(document).ready(function(){
	$("#search").click(function(){
		// alert($("#searchdate").val());
		getList();
	});
	var date = new Date();
    var year  = date.getFullYear();
    var month = date.getMonth() + 1; // 0부터 시작하므로 1더함 더함
    var day   = date.getDate();

    if (("" + month).length == 1) { month = "0" + month; }
    if (("" + day).length   == 1) { day   = "0" + day;   }
	$("#searchdate").val(year+"-"+month+"-"+day);
	getList();

});

function getList(){
	var searchdate = $("#searchdate").val();
	$.ajax({
		type : 'GET',
		url : '/pm25/getlist',
		data : {searchdate:searchdate},
		dataType : 'text',
		success : function(data) {
			$("#listbody").html(data);
		}
	})	
}

</script>
</head>
<body>
	<div data-role="page" id="pageone">
		<div data-role="header"><% include
			../public/include/pageHeader.html %></div>
		<div data-role="main" class="ui-content">
			<div class='row'>
				<div class="col-md-3"><input type="date" name="searchdate" id="searchdate"></div>
				<div class="col-md-3"><button type="button" class="btn btn-primary" id="search">
				<span class="glyphicon glyphicon-search"></span>검색</button></div>
			</div>
			<hr/>
			<div class='row'>
				<div id='listbody'><h4>데이터 로딩에 다소 시간이 소요됩니다. 잠시만 기다리세요</h3></div>
			</div>
			
		</div>
		<div data-role="footer"><% include
			../public/include/pageFooter.html %></div>
	</div>
</body>
</html>